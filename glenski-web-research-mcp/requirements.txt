# web-research-mcp dependencies
# Install: pip install -r requirements.txt

mcp>=1.9,<2                 # Model Context Protocol SDK
ddgs>=9,<10                 # DuckDuckGo search, no API key required
httpx>=0.27,<1              # HTTP client
beautifulsoup4>=4.12,<5     # HTML parsing and content extraction
cryptography<49; sys_platform == "darwin"  # Preserve Intel macOS wheel support
